package Algorithms.Two_sum2;

public class Solution {
    public int[] twoSum(int[] numbers, int target) {
        int[] ret = new int[2];
        int x;
        int half;
        for(int i = 0; i < numbers.length; i++)
        {
             half = (int)((numbers.length - 1) / 2); 
        }
        
        return ret;
    }
}
